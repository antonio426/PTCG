# Copy this file to `.env` (same directory) and fill in your own key to enable the "hard"
# (ClaudeAI) difficulty option. Never commit the real `.env` file.
ANTHROPIC_API_KEY=
# Optional — overrides the default model id used by ClaudeAI.
ANTHROPIC_MODEL=
